package alien.api;

/**
 * Flag the Request as one-way objects, not expecting a reply to them
 * 
 * @author costing
 * @since 2015-10-14
 */
public interface OneWayMessage {
	// empty interface, just declaring that a Request implements it is enough
}
